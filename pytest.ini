[pytest]
pythonpath = . src
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: mark test as integration test (requires full stack)
    network: mark test as requiring network access
    slow: mark test as slow running
