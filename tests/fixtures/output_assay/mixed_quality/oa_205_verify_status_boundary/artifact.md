`verify` is about object truth. `status` is about reliance posture.
Action: run `verify` before using `status` in a reviewer-facing flow.
That separation guarantees every PASS status means the artifact is semantically correct.
