Observation is not assertion.
Margin note: "Ready to ship."
A `claim.observation_promoted` receipt MUST reference `run_id`, `unit_id`, `artifact_hash`, and `artifact_span` from the source observation.
That provenance rule means the operator can trust every future promotion automatically.
