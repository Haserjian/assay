{
  "format_version": "0.1.0",
  "specimen": "path_traversal",
  "description": "Manifest files array includes a path traversal entry. Manifest re-signed.",
  "mutation": "files: appended {'path': '../../../etc/passwd', ...}. Re-signed.",
  "expected_verification": {
    "passed": false,
    "primary_error": {
      "code": "E_PATH_ESCAPE",
      "field": "../../../etc/passwd",
      "verification_step": "step_0_containment_check"
    }
  },
  "conformance_requirement": "A conforming verifier MUST report passed=false with at least one E_PATH_ESCAPE. No filesystem read should occur for the traversal path."
}